export interface CheckWebPageInput {
  /**
   * URL of the Web Page to check
   */
  webPageUrl: string;
  /**
   * Email address to be notified when changes are detected
   */
  notificationEmail: string;
}
